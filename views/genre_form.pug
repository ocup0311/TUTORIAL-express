extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='name') 類別：
      input#name.form-control(type='text', placeholder='如科學、小說 等' name='name' value=(undefined===genre ? '' : genre.name))
    button.btn.btn-primary(type='submit') 確認送出

  if errors 
   ul
    for error in errors
     li!= error.msg