extends layout

block content
  h1= title

  ul
    each val in bookinstance_list
      li 
        a(href=val.url) #{val.book.title} : #{val.imprint} -      
        if val.status==='Available'
          span.text-success #{val.statusName}
        else if val.status==='Maintenance'
          span.text-danger #{val.statusName}
        else
          span.text-warning #{val.statusName} 
        //- if val.status!='Available'
        //-   span  (Due: #{val.due_back} )
        if val.status != 'Available'
          //span  (Due: #{val.due_back} )
          span  (#[small 預期歸還：] #{val.dueBackFormatted} )    
    else
      li 目前本館沒有任何藏書，持續擴建中，敬請期待。